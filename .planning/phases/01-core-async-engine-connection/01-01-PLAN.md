---
phase: 01-core-async-engine-connection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [trade_harness/__init__.py, trade_harness/config.py, trade_harness/connection/binance_ws.py, trade_harness/main.py, .env.example]
autonomous: true
requirements: [CONN-01]

must_haves:
  truths:
    - "Bot connects to Binance Futures WebSocket bookTicker and prints live tick data without blocking"
  artifacts:
    - path: "trade_harness/config.py"
      provides: "Environment variables and configuration loading"
    - path: "trade_harness/connection/binance_ws.py"
      provides: "Binance WebSocket client stream listener"
    - path: "trade_harness/main.py"
      provides: "Main asyncio event loop launcher"
  key_links:
    - from: "trade_harness/main.py"
      to: "trade_harness/connection/binance_ws.py"
      via: "import and invocation in event loop"
---

<objective>
Thiết lập khung cấu trúc dự án, file cấu hình môi trường, và hiện thực hóa WebSocket client lắng nghe dữ liệu bookTicker BTCUSDT từ Binance Futures một cách bất đồng bộ.

Output: 
- `trade_harness/config.py` nạp cấu hình từ file `.env`
- `trade_harness/connection/binance_ws.py` chứa client WebSocket async
- `trade_harness/main.py` làm core loop khởi chạy hệ thống
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-async-engine-connection/01-CONTEXT.md
@.planning/phases/01-core-async-engine-connection/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold python package structure and config nạp .env</name>
  <files>trade_harness/__init__.py, trade_harness/config.py, .env.example</files>
  <read_first>trade_harness/config.py</read_first>
  <action>Tạo thư mục trade_harness và file __init__.py. Viết trade_harness/config.py định nghĩa các cấu hình: BINANCE_WS_URL, LM_STUDIO_URL, API_KEY/SECRET và nạp chúng sử dụng os.getenv. Tạo file .env.example mẫu.</action>
  <verify>python3 -c "import trade_harness.config as cfg; print(cfg.BINANCE_WS_URL)"</verify>
  <acceptance_criteria>
    - trade_harness/config.py định nghĩa và export BINANCE_WS_URL mặc định là "wss://fstream.binance.com/ws/btcusdt@bookTicker"
  </acceptance_criteria>
  <done>Cấu hình được load thành công</done>
</task>

<task type="auto">
  <name>Task 2: Build Binance WebSocket connection client</name>
  <files>trade_harness/connection/binance_ws.py</files>
  <read_first>trade_harness/connection/binance_ws.py</read_first>
  <action>Xây dựng lớp BinanceWSClient sử dụng thư viện websockets (hoặc aiohttp websockets) để kết nối wss://fstream.binance.com/ws/btcusdt@bookTicker, nạp tin nhắn bất đồng bộ và log dữ liệu thô (best_bid/best_ask).</action>
  <verify>python3 -c "from trade_harness.connection.binance_ws import BinanceWSClient; import asyncio; print(BinanceWSClient)"</verify>
  <acceptance_criteria>
    - binance_ws.py chứa hàm loop `async def start_listening()` hoặc class tương đương sử dụng async loop để đọc tin nhắn từ socket.
  </acceptance_criteria>
  <done>Client WebSocket viết xong và import được</done>
</task>

<task type="auto">
  <name>Task 3: Create main entry point with asyncio loop runner</name>
  <files>trade_harness/main.py</files>
  <read_first>trade_harness/main.py</read_first>
  <action>Xây dựng main.py khởi chạy task start_listening() bằng asyncio.run(). Đảm bảo chương trình bắt được KeyboardInterrupt để tắt an toàn.</action>
  <verify>python3 -m trade_harness.main (chạy thử trong 5 giây rồi tắt bằng SIGINT)</verify>
  <acceptance_criteria>
    - main.py chứa điểm chạy `asyncio.run`
  </acceptance_criteria>
  <done>Chương trình khởi chạy kết nối thành công</done>
</task>

</tasks>

<verification>
- [ ] Khởi chạy python3 -m trade_harness.main kết nối thành công và in log tick từ Binance.
- [ ] Code syntax và type checking không có lỗi.
</verification>

<success_criteria>
- Thiết lập xong core WebSocket thu thập dữ liệu tick data real-time BTCUSDT.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-async-engine-connection/01-01-SUMMARY.md`
</output>
