---
phase: 01-core-async-engine-connection
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [trade_harness/core/portfolio.py, trade_harness/connection/binance_rest.py, trade_harness/connection/binance_ws.py, trade_harness/main.py]
autonomous: true
requirements: [CONN-02]

must_haves:
  truths:
    - "Portfolio Cache is updated from tick data in real-time"
    - "NETWORK_BLIND flag is set immediately on WebSocket disconnect, triggering REST API polling every 1s"
  artifacts:
    - path: "trade_harness/core/portfolio.py"
      provides: "In-memory Portfolio State cache class"
    - path: "trade_harness/connection/binance_rest.py"
      provides: "REST API fallback polling client"
  key_links:
    - from: "trade_harness/connection/binance_ws.py"
      to: "trade_harness/core/portfolio.py"
      via: "cache updates during tick reception"
    - from: "trade_harness/connection/binance_ws.py"
      to: "trade_harness/connection/binance_rest.py"
      via: "fallback triggering when socket disconnects"
---

<objective>
Xây dựng lớp PortfolioCache quản lý trạng thái tài khoản trong RAM, và tích hợp bộ REST API Fallback phục vụ lấy giá khi mất mạng cùng cơ chế Circuit Breaker NETWORK_BLIND.

Output:
- `trade_harness/core/portfolio.py` lưu trữ trạng thái tài khoản, vị thế và giá thị trường trong RAM cache
- `trade_harness/connection/binance_rest.py` chứa hàm fallback GET REST API bookTicker để cập nhật giá
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-async-engine-connection/01-CONTEXT.md
@.planning/phases/01-core-async-engine-connection/01-RESEARCH.md
@.planning/phases/01-core-async-engine-connection/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PortfolioState RAM cache</name>
  <files>trade_harness/core/portfolio.py</files>
  <read_first>trade_harness/core/portfolio.py</read_first>
  <action>Hiện thực hóa class PortfolioCache lưu trữ available_balance, locked_margin, positions, open_orders, best_bid, best_ask, timestamp, và cờ network_blind (mặc định False). Thêm các method thread-safe đơn giản để update dữ liệu.</action>
  <verify>python3 -c "from trade_harness.core.portfolio import PortfolioCache; cache = PortfolioCache(); print(cache.network_blind)"</verify>
  <acceptance_criteria>
    - portfolio.py chứa class PortfolioCache và cấu trúc dữ liệu theo thiết kế schema
  </acceptance_criteria>
  <done>PortfolioCache viết xong</done>
</task>

<task type="auto">
  <name>Task 2: Implement REST API price polling fallback client</name>
  <files>trade_harness/connection/binance_rest.py</files>
  <read_first>trade_harness/connection/binance_rest.py</read_first>
  <action>Xây dựng module binance_rest.py sử dụng aiohttp hoặc httpx gửi request GET đến endpoint công khai `https://fapi.binance.com/fapi/v1/ticker/bookTicker?symbol=BTCUSDT` để lấy best bid/ask.</action>
  <verify>python3 -c "import asyncio; from trade_harness.connection.binance_rest import fetch_book_ticker; print(asyncio.run(fetch_book_ticker()))"</verify>
  <acceptance_criteria>
    - binance_rest.py chứa hàm `fetch_book_ticker` trả về dict giá bid/ask hoặc nạp thẳng vào Cache
  </acceptance_criteria>
  <done>REST client viết xong</done>
</task>

<task type="auto">
  <name>Task 3: Connect WebSocket disconnect logic with Circuit Breaker and Fallback polling</name>
  <files>trade_harness/connection/binance_ws.py, trade_harness/main.py</files>
  <read_first>trade_harness/connection/binance_ws.py, trade_harness/main.py</read_first>
  <action>Cập nhật binance_ws.py và main.py: nạp PortfolioCache dùng chung. Khi WebSocket bị đứt (`ConnectionClosed`), gán `cache.network_blind = True` và tự động khởi động một task chạy ngầm gọi REST API fallback mỗi 1 giây. Khi kết nối lại được, gán `cache.network_blind = False` và hủy task ngầm.</action>
  <verify>Tắt mạng cục bộ (hoặc mô phỏng lỗi ngắt WS), kiểm tra log xem cờ NETWORK_BLIND bật lên và hệ thống kích hoạt polling REST API thay thế.</verify>
  <acceptance_criteria>
    - main.py hoặc binance_ws.py bắt lỗi ngắt kết nối WebSocket và kích hoạt task fallback REST
  </acceptance_criteria>
  <done>Cơ chế Circuit Breaker và Fallback hoạt động đúng</done>
</task>

</tasks>

<verification>
- [ ] Code build thành công và cache cập nhật đúng giá từ Websocket.
- [ ] Circuit breaker kích hoạt REST fallback cập nhật giá khi WebSocket mất kết nối.
</verification>

<success_criteria>
- Portfolio Cache hoạt động đúng, tự động kích hoạt chế độ NETWORK_BLIND và gọi REST API cứu giá khi rớt mạng.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-async-engine-connection/01-02-SUMMARY.md`
</output>
