---
phase: 01-core-async-engine-connection
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [trade_harness/inference/lm_studio.py, trade_harness/core/context.py, trade_harness/main.py]
autonomous: true
requirements: [CONN-03, CONT-01, CONT-02]

must_haves:
  truths:
    - "System generates context JSON containing best bid/ask and available balance"
    - "System can perform async OpenAI-compatible completions calling local LM Studio"
  artifacts:
    - path: "trade_harness/inference/lm_studio.py"
      provides: "Async client client connection to LM Studio"
    - path: "trade_harness/core/context.py"
      provides: "Context Harness prompt serialization"
  key_links:
    - from: "trade_harness/main.py"
      to: "trade_harness/core/context.py"
      via: "context builder query"
    - from: "trade_harness/main.py"
      to: "trade_harness/inference/lm_studio.py"
      via: "passing prompt context to LLM client completions"
---

<objective>
Xây dựng Context Harness biên dịch Portfolio State thành JSON String và prompt hoàn chỉnh. Thiết lập Async client kết nối LM Studio (localhost:1234) chạy Gemma 2 9B để nội suy tín hiệu không bị block thread.

Output:
- `trade_harness/core/context.py` serialize trạng thái làm prompt cho LLM
- `trade_harness/inference/lm_studio.py` client gọi LM Studio async
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-async-engine-connection/01-CONTEXT.md
@.planning/phases/01-core-async-engine-connection/01-RESEARCH.md
@.planning/phases/01-core-async-engine-connection/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ContextHarness prompt generator</name>
  <files>trade_harness/core/context.py</files>
  <read_first>trade_harness/core/context.py</read_first>
  <action>Hiện thực hóa class ContextHarness. Viết method `build_json_context(cache: PortfolioCache)` để serialize state thành cấu trúc data contract. Viết method `build_prompt(cache: PortfolioCache)` đóng gói prompt với hướng dẫn Gemma 2 9B trả về định dạng Signal JSON (Action, Confidence, SL, TP).</action>
  <verify>python3 -c "from trade_harness.core.context import ContextHarness; from trade_harness.core.portfolio import PortfolioCache; print(ContextHarness().build_prompt(PortfolioCache()))"</verify>
  <acceptance_criteria>
    - context.py xuất class ContextHarness định dạng đúng JSON schema
  </acceptance_criteria>
  <done>ContextHarness viết xong</done>
</task>

<task type="auto">
  <name>Task 2: Implement LM Studio Async Client</name>
  <files>trade_harness/inference/lm_studio.py</files>
  <read_first>trade_harness/inference/lm_studio.py</read_first>
  <action>Xây dựng class LMStudioClient sử dụng thư viện `openai` AsyncOpenAI. Cấu hình base_url là `http://localhost:1234/v1` và api_key="lm-studio" (mặc định của LM Studio). Viết hàm async `generate_decision(prompt: str)` để lấy completions.</action>
  <verify>python3 -c "from trade_harness.inference.lm_studio import LMStudioClient; import asyncio; print(LMStudioClient)"</verify>
  <acceptance_criteria>
    - lm_studio.py sử dụng `AsyncOpenAI` để tạo async request
  </acceptance_criteria>
  <done>Client LM Studio viết xong</done>
</task>

<task type="auto">
  <name>Task 3: Connect LLM inference loop in main event loop</name>
  <files>trade_harness/main.py</files>
  <read_first>trade_harness/main.py</read_first>
  <action>Cập nhật main.py chạy thêm một background loop phụ định kỳ (ví dụ: mỗi 5-10 giây) thò tay vào PortfolioCache lấy dữ liệu giá, dựng prompt và gọi LM Studio lấy tín hiệu giao dịch. Kiểm tra xem WebSocket vẫn nhận được tick bình thường trong khi LLM đang chạy (không bị nghẽn).</action>
  <verify>Khởi chạy hệ thống, xác minh WebSocket liên tục nhận tick, và song song đó cứ 5 giây hệ thống gửi context thành công và nhận tín hiệu từ LM Studio (hoặc giả lập LM Studio mock endpoint nếu LM Studio chưa mở).</verify>
  <acceptance_criteria>
    - main.py phối hợp async Websocket loop và async LLM calling loop không chặn nhau
  </acceptance_criteria>
  <done>LLM inference được tích hợp async thành công</done>
</task>

</tasks>

<verification>
- [ ] Dựng prompt đúng định dạng data contract.
- [ ] Chạy thử nghiệm thành công cuộc gọi async không block thread chính.
</verification>

<success_criteria>
- Đóng gói xong Context Harness và kết nối thành công LM Studio offline.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-async-engine-connection/01-03-SUMMARY.md`
</output>
